{
  "name": "@da/domain",
  "version": "1.0.0",
  "private": true,
  "description": "Shared domain model for Dijital Asistan — entities, enums, source references, AI output contracts, API contracts.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "^3.2.4"
  }
}
