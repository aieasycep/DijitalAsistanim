{
  "name": "@da/api-client",
  "version": "1.0.0",
  "private": true,
  "description": "Client data layer for Dijital Asistan: a single DataSource interface with a Supabase adapter (production) and a deterministic Demo adapter (development), plus TanStack Query keys.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@da/domain": "workspace:*",
    "@da/validation": "workspace:*",
    "@supabase/supabase-js": "^2.115.0",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.19.0",
    "typescript": "5.9.3",
    "vitest": "^3.2.4"
  }
}
