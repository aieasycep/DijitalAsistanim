{
  "name": "@da/design-tokens",
  "version": "1.0.0",
  "private": true,
  "description": "Dijital Asistan design tokens — single source of truth for color, type, spacing, radius, shadow, motion (light + dark).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "^3.2.4"
  }
}
